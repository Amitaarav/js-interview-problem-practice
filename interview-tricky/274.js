const yt = "FrontendMaster";
const ig = "Allahabadi.dev";

const result = (ig && yt);
console.log(result); // Allahabadi.dev

/*If the first operand is falsy, it returns that operand and stops evaluating.
If the first operand is truthy, it evaluates and returns the second operand.
*/